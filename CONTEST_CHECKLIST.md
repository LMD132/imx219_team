# 赛题四 需求逐条对照表

需求来源：《2026 FPGA竞赛赛题指南 -8.22(更正版).pdf》第 23–26 页。
证据等级按 `AGENTS.md` 的约定区分：**RTL 实现 / 编译通过 / JTAG 下载 / 肉眼画面确认**。本表只写已核实的事实，未观察到的硬件效果一律标"未确认"。

核对时间：2026-09-24，对照 `main`（`fa1ddcb` 及之后）的源码与约束。

## 一、基础要求（PDF 第 25 页）

### 1. 摄像头驱动与图像采集

| 细则 | 状态 | 证据 / 缺口 |
| --- | --- | --- |
| 分辨率 640×480 或以上 | ✅ 满足 | IMX219 经 2-lane MIPI CSI-2 采集 1280×720，`rtl/csi_rx_controller.sv` + `rtl/cam/piv2_720p_7M_2L_reg.mem` |
| 采集数据存入行缓存（FIFO 或 Block RAM） | ✅ 满足 | `rtl/edge_display_720p.v` 用 `line_a` / `line_b` 两行缓存；链路另有 `dual_clock_fifo` 与 DDR 帧缓存 |
| 采集稳定，无丢帧、无错位 | ⚠️ 未确认 | 只做到"肉眼看着实时变化"，**没有丢帧计数、长时间观测记录或错位检查**。要补证据 |

### 2. 彩色图像转灰度

| 细则 | 状态 | 证据 / 缺口 |
| --- | --- | --- |
| 用 DSP 或移位加法实现乘法，避免除法器 | ✅ 满足 | `rtl/edge_display_720p.v:24-33`：`Y = (77R + 150G + 29B) >> 8`，全部展开成移位+加法（×77=<<6+<<3+<<2+1，×150=<<7+<<4+<<2+<<1，×29=<<4+<<3+<<2+1），无除法器 |
| 灰度作为边缘检测的前置步骤 | ✅ 满足 | 灰度 → （中值，实验分支）→ Sobel 流水 |

### 3. Sobel 边缘检测算法

| 细则 | 状态 | 证据 / 缺口 |
| --- | --- | --- |
| 3×3 Sobel，计算 Gx 和 Gy | ✅ 满足 | `rtl/edge_display_720p.v` 的 `gx_positive/negative`、`gy_positive/negative` |
| 卷积核 Gx=[-1,0,+1;-2,0,+2;-1,0,+1]、Gy=[-1,-2,-1;0,0,0;+1,+2,+1] | ✅ 满足 | 代码中权重为 1 / 2 / 1，符号分离成 positive / negative 两组，与题面一致 |
| 梯度幅值近似 G=\|Gx\|+\|Gy\|（避免开方） | ✅ 满足 | `gx_abs` / `gy_abs` 后相加得 `magnitude` |
| 阈值比较输出二值化边缘图，阈值可参数化配置 | ✅ 满足（静态） | 参数 `EDGE_THRESHOLD`，当前 `main` 为 11'd180；可参数化但**不能在运行中调整**（见高阶第 2 项） |
| 设计行缓存模块缓存 2 行，实现 3×3 滑动窗口 | ✅ 满足 | `line_a` / `line_b` + 三行移位寄存器 `top/mid/bot` |

### 4. HDMI 显示驱动

| 细则 | 状态 | 证据 / 缺口 |
| --- | --- | --- |
| 生成标准 HDMI 时序（最低 640×480@60） | ✅ 满足 | 1280×720@60 标准 CEA 时序，经 LVDS 伪 TMDS 输出 |
| 分屏显示：左（上）原始灰度、右（下）边缘结果 | ⚠️ 形式满足、口径待改 | 代码按 `s1_x < 640` 左半显示灰度、右半显示二值边缘。**但两半各显示自己的空间裁切**（左半区像素、右半区像素），不是"同一视野的灰度与边缘并排"。作者已注明 `// the two halves currently show their own crop` |
| 或实现画中画叠加显示 | ❌ 未做 | — |
| 显示稳定，无闪烁、无撕裂 | ⚠️ 未确认 | 无专项观测或录像证据 |

### 5. 基础验证

| 细则 | 状态 | 证据 / 缺口 |
| --- | --- | --- |
| 提交完整 RTL 源码（Verilog/VHDL）和约束文件 | ✅ 满足 | 仓库 `rtl/`、`ti60f225_oob.peri.xml`、`ti60f225_oob.sdc`、`ti60f225_oob.xml` |
| 提供演示视频（黑白棋盘格 / 印数字的纸 / 人脸轮廓） | ❌ 未做 | 需要用摄像头对着上述目标实拍 |

## 二、高阶挑战（PDF 第 26 页）

| # | 项目 | 状态 | 证据 / 缺口 |
| --- | --- | --- | --- |
| 1 | 中值滤波去噪（灰度后、边缘前插 3×3 中值；9 像素排序取中位；并行比较网络避免串行排序） | 🔄 已实现未验收 | `median-experiment` 分支 `rtl/median_filter_3x3_720p.v`：两行缓存 + 9 轮奇偶比较交换网络，非串行排序，符合"并行比较网络"要求。编译/时序/JTAG 均成功；**画面右侧丢了很多边缘，判为未通过** |
| 2 | 实时阈值调节（按键 ± 调整，含消抖与阈值寄存器） | ❌ 未做 | 顶层目前没有任何按键输入端口，只有 `i_arstn` 复位。硬件上 KEY0 已引到 `GPIOL_07`（现被 `i_arstn` 占用），另可加 KEY1=GPIOR_22、KEY2=GPIOR_21、KEY3=GPIOL_03 |
| 3 | 帧缓存与 DDR 控制（整帧写入外部 DDR，采集-存储-读取-处理分离；回放 / 帧冻结 / 多帧对比） | 🔄 部分 | `rtl/ddr3_controller/ddr3_top.v` + `rtl/frame_buffer_V4/frame_buffer.v` 已在通路上，整帧经 DDR 中转成立。**回放、帧冻结、多帧对比未做**。注意：整帧缓存会引入约 1 帧延迟，与题面"零延迟"的表述冲突，演示口径要先想清楚 |
| 4 | Canny 边缘检测（高斯滤波→幅值+方向→非极大值抑制→双阈值→滞后连接） | ❌ 未做 | — |
| 5 | 边缘叠加彩色输出（红色边缘线叠加到原始彩色图） | ❌ 未做 | 当前显示只有灰度通道与黑白二值，没有彩色叠加通路；`edge_display_720p` 的输出是 R=G=B |
| 6 | 简易目标识别（圆形/矩形，屏幕提示"检测到圆形"） | ❌ 未做 | — |

> 题面补充：进阶项"完成度越高，得分越高。算法复杂度和视频帧频也是考核维度"。

## 三、汇总

- **基础要求**：5 大项 12 条细则，**8 条已完成**（采集/行缓存/灰度/3×3 Sobel/幅值近似/参数化阈值/HDMI 时序/RTL 与约束交付），**3 条待补证据或改口径**（无丢帧证据、分屏口径、显示稳定性），**1 条未做**（演示视频）。
- **高阶挑战**：6 项中 **0 项完成**，1 项（中值滤波）已实现待验收，1 项（DDR 帧缓存）核心成立但扩展全缺，4 项未开始。
- 结论：**基础部分基本成立，缺"证据"和"展示"；高阶部分才刚开头，是当前提分的主要空间。**

## 四、更新方式

每完成或推翻一条，就改本表对应行，并在提交说明里写清证据等级（RTL 实现 / 编译通过 / JTAG 下载 / 肉眼画面）。不要用"编译通过"代替"画面确认"。
