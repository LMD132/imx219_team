# 赛题四 需求逐条对照表

需求来源：《2026 FPGA竞赛赛题指南 -8.22(更正版).pdf》第 23–26 页。
证据等级按 `AGENTS.md` 的约定区分：**RTL 实现 / 编译通过 / JTAG 下载 / 肉眼画面确认**。
本表只写已核实的事实，未观察到的硬件效果一律标"未确认"。

核对时间：2026-09-26，分支 `py-algo-rtl`（算法全链 RTL 化 + 逐位对拍 + 编译 PASS）。
算法溯源与详细推导见 [`docs/ALGO_RTL.md`](docs/ALGO_RTL.md)。

> **算法来源说明**：本分支的算法**完全移植**自 GitHub 仓库
> [liuziyaoyao1210-sudo/FPGA-Python](https://github.com/liuziyaoyao1210-sudo/FPGA-Python)
> 的 `edge_pipeline.py`，未参考其它工程的算法实现。Python 与 RTL 的逐级映射、
> 以及"逐位一致"的对拍数据见 `docs/ALGO_RTL.md` §3、§4。

---

## 一、基础要求（PDF 第 25 页）

### 1. 摄像头驱动与图像采集

| 细则 | 状态 | 证据 / 缺口 |
| --- | --- | --- |
| 分辨率 640×480 或以上 | ✅ 满足 | IMX219 经 2-lane MIPI CSI-2 采集 1280×720，`rtl/csi_rx_controller.sv` + `piv2_720p_7M_2L_reg.mem` |
| 采集数据存入行缓存（FIFO 或 Block RAM） | ✅ 满足 | 算法侧 `rtl/algo/alg_win.v` 每个窗口算子都有独立行缓存；链路侧另有 `dual_clock_fifo` 与 DDR 帧缓存 |
| 采集稳定，无丢帧、无错位 | ⚠️ **未确认** | 只有"肉眼看实时变化"，**没有丢帧计数、长时间观测记录或错位检查**。这是基础要求里最大的证据缺口 |

### 2. 彩色图像转灰度

| 细则 | 状态 | 证据 / 缺口 |
| --- | --- | --- |
| 用 DSP 或移位加法实现乘法，避免除法器 | ✅ 满足 | `rtl/algo/alg_gray.v`：`Y=(77R+150G+29B)>>8`，系数展开成移位加法（77=64+8+4+1，150=128+16+4+2，29=32-4+1），无除法器无乘法器 |
| 灰度作为边缘检测的前置步骤 | ✅ 满足 | `alg_gray` →（`alg_median3` 可选）→ `alg_gauss5` 可选 → `alg_sobel3` |
| 与 Python 算法逐位一致 | ✅ 满足 | `check_py_repo.py`：`to_gray` 在 2 张图上 0 mismatch / 76800 与 33280 |

### 3. Sobel 边缘检测算法

| 细则 | 状态 | 证据 / 缺口 |
| --- | --- | --- |
| 3×3 Sobel，计算 Gx 和 Gy | ✅ 满足 | `rtl/algo/alg_sobel3.v`（`out_gx` / `out_gy` 亦有输出） |
| 卷积核 Gx=[-1,0,+1;-2,0,+2;-1,0,+1]、Gy=[-1,-2,-1;0,0,0;+1,+2,+1] | ✅ 满足 | 权重 1/2/1，符号分离成正负两组，与题面一致 |
| 梯度幅值近似 G=\|Gx\|+\|Gy\|（避免开方） | ✅ 满足 | `out_mag = |Gx| + |Gy|`，全量程 11bit，无开方 |
| 阈值比较输出二值化边缘图，阈值可参数化配置 | ✅ 满足（**运行期可调**） | `alg_thresh.v` 的 `cfg_t` / `cfg_lo` / `cfg_hi` 是**运行期输入端口**（顶层寄存器），不是综合期常量；支持单阈值、双阈值滞后、防呆自动交换 |
| 设计行缓存模块缓存 2 行，实现 3×3 滑动窗口 | ✅ 满足 | `rtl/algo/alg_win.v`：每行一块真双口 RAM + 列移位寄存器 |

### 4. HDMI 显示驱动

| 细则 | 状态 | 证据 / 缺口 |
| --- | --- | --- |
| 生成标准 HDMI 时序（最低 640×480@60） | ✅ 满足（链路未改动） | 1280×720@60 标准 CEA 时序，经 LVDS 伪 TMDS 输出；本分支未改 `dvi_encoder` 及前端 |
| 分屏显示：左（上）原始灰度、右（下）边缘结果 | ✅ 已修正口径 | 顶层**固定** `DISP_MODE=2'd0`：**同一完整视野** 2:1 水平抽取，左半=灰度全画幅、右半=边缘全画幅。修正了旧版"两个半边各显示自己的空间裁切"的问题；原先的模式轮换已按需求移除 |
| 或实现画中画叠加显示 | ❌ 未做 | 已实现的是左右分屏与全屏，画中画（PiP）未做 |
| 显示稳定，无闪烁、无撕裂 | ⚠️ **未确认** | 无专项观测或录像证据。**注意**：本算法的帧间白点闪烁是已知现象，仓库自述里 `temporal_blend`(TEMP) 是最直接的解法，但尚未 RTL 化（见 §二 #5 注） |

### 5. 基础验证

| 细则 | 状态 | 证据 / 缺口 |
| --- | --- | --- |
| 提交完整 RTL 源码（Verilog/VHDL）和约束文件 | ✅ 满足 | `rtl/`（含 `rtl/algo/` 11 个算法模块）、`ti60f225_oob.peri.xml`、`ti60f225_oob.sdc`、`ti60f225_oob.xml` |
| 提供演示视频（黑白棋盘格 / 印数字的纸 / 人脸轮廓） | ❌ 未做 | 需要用摄像头对着上述目标实拍；依赖上板 |

---

## 二、高阶挑战（PDF 第 26 页）

| # | 项目 | 状态 | 证据 / 缺口 |
| --- | --- | --- | --- |
| 1 | 中值滤波去噪（灰度后、边缘前插 3×3 中值；9 像素排序取中位；并行比较网络避免串行排序） | ✅ **RTL 完成 + 逐位对拍**（待上板） | `rtl/algo/alg_median3.v`：**19 个 2 输入比较器**的 3 级排序网络，非串行排序。逐位对拍 2 图 0 mismatch。运行期开关 `cfg_median_en`（关掉时延迟不变，画面不跳） |
| 2 | 实时阈值调节（按键 ± 调整，含消抖与阈值寄存器） | 🔄 **运行期可调（PC 串口界面）；板上按键未接入** | 9 个参数（`cfg_mode/t/lo/hi/median_en/gauss_en/isol_en/disp_mode/ov_color`）已是运行期端口，现在由板载 UART 驱动：`tools/alg_tuner.py` 拖动滑块 → 画面**立即**变化，**无需重新编译或烧录**（真板实测：串口通道 9/9 OK、GUI 端到端 PASS，见 `docs/运行期调参.md`）。**仍缺**：题面字面要求的**板上按键 ± 与消抖** —— 顶层无按键输入（`GPIOL_07` 被 `i_arstn` 占用），也未写消抖逻辑。可用 `GPIOR_22`/`GPIOR_21`/`GPIOL_03`，需改 `peri.xml`（属"明确板级证据+队员同意"才动） |
| 3 | 帧缓存与 DDR 控制（整帧写入外部 DDR，采集-存储-读取-处理分离；回放 / 帧冻结 / 多帧对比） | 🔄 部分 | `rtl/ddr3_controller/ddr3_top.v` + `rtl/frame_buffer_V4/frame_buffer.v` 已在通路上，整帧经 DDR 中转成立。**回放、帧冻结、多帧对比未做**。整帧缓存会引入约 1 帧延迟，与"零延迟"表述冲突，演示口径要先想清楚 |
| 4 | Canny 边缘检测（高斯滤波→幅值+方向→非极大值抑制→双阈值→滞后连接） | ✅ **RTL 完成 + 逐位对拍**（待上板） | 完整链：`alg_gauss5`（5×5 整数高斯）→ `alg_sobel3`（幅值 + 方向量化）→ `alg_nms`（非极大值抑制）→ `alg_thresh`（双阈值滞后）→ `alg_despeckle`（去孤点）。7 级 × 3 种模式全 0 mismatch。参数 = 仓库定稿 58/21 |
| 5 | 边缘叠加彩色输出（红色边缘线叠加到原始彩色图） | ✅ **RTL 完成**（待上板） | `alg_vdisp` mode 1：`ov_color=1` 时彩色原图 + 红边；`ov_color=0` 时灰度底 + 红边。注：仓库 `config.json` 的 `color=0` 是灰度底，板上演示设成 1 更直观 |
| 6 | 简易目标识别（圆形/矩形，屏幕提示"检测到圆形"） | ❌ 未做 | 仓库 Python 有 `detect_shapes()`/`draw_shapes()`，RTL 侧未实现（轮廓提取 + 凸性判定 + 字库，工作量大） |

> 题面补充：进阶项"完成度越高，得分越高。算法复杂度和视频帧频也是考核维度"。
>
> **注**：仓库定稿参数含 `temp=50`（时间域帧间平均 `temporal_blend`），是压制帧间白点闪烁最直接的手段，
> 但未 RTL 化（需整帧缓存，而当前 Memory Blocks 已用 81.25%）。这是**下一步最高优先级的功能缺口**。

---

## 三、汇总

- **基础要求**：5 大项 15 条细则，**11 条已完成**。待补：丢帧/错位证据、显示稳定性证据、
  画中画、演示视频。分屏口径问题已在本分支修正为"同一完整视野"。
- **高阶挑战**：6 项中 **4 项 RTL 完成并逐位对拍**（①中值 ④Canny ⑤彩色叠加，以及
  ②的阈值寄存器通路）。**② 已升级为"运行期可调"**：9 个参数经板载 UART 由
  `tools/alg_tuner.py` 实时驱动，拖动即生效、无需重新编译/烧录，且有真板实测证据；
  ② 剩下的缺口只有"板上按键 ± 与消抖"这一形式要求。1 项部分（③DDR 帧缓存扩展），
  1 项未做（⑥目标识别）。
- **证据等级**：以上全部为 **RTL 实现 + 仿真逐位对拍 + 编译/时序通过**；
  其中算法全链位流已于 2026-09-26 **JTAG 下载成功**，且运行期调参通道已有
  **真板实测**（串口命令 9/9 OK、GUI 端到端 PASS，见 `docs/运行期调参.md`）。
  **上板肉眼画面确认仍未做**，任何"画面效果"结论都还需实物验证。
  > 调参通道的 9/9 证明的是"参数确实进了 FPGA 并回读一致"，**不等于**"画面按预期变化"——
  > 后者仍属肉眼观测项，见 `docs/上板记录_2026-09-26.md` §5。
- 结论：**算法侧（基础 + 高阶核心项）已达到可上板验证的状态；下一步瓶颈在"上板取证"与
  "演示视频"，而不是算法实现。**

---

## 四、更新方式

每完成或推翻一条，就改本表对应行，并在提交说明里写清证据等级
（RTL 实现 / 编译通过 / JTAG 下载 / 肉眼画面）。不要用"编译通过"代替"画面确认"。
