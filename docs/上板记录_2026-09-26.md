# 上板记录 · 赛题4 算法全链位流（2026-09-26）

本文件按 `AGENTS.md` 的证据分级记录，**JTAG 下载**与**肉眼画面确认**分开写，
不要互相替代。

---

## 1. 本次做了什么

| 项 | 值 |
| --- | --- |
| 日期 | 2026-09-26 |
| 位流 | `candidate_bitstreams/algo_canny_full_20260926_1954.bit` |
| 字节数 | 2422623 |
| SHA-256 | `0B1354A8C08C544B40801815378BCD93DB604210A1BC1CC805BE2592CC0AED89`（烧录前已核对一致） |
| 来源提交 | `4fcc6dbef0bce2090ab934cc1ab13f63c84f6c51`（分支 `py-algo-rtl`） |
| 方式 | JTAG（`-m jtag`），**易失**，非 Flash 烧写 |
| 命令 | 见 §4 |

---

## 2. 证据等级更新

| 等级 | 之前 | 现在 |
| --- | --- | --- |
| A. RTL 实现 | ✅ | ✅ |
| B. 仿真逐位对拍 | ✅ | ✅ |
| C. Python 溯源对拍 | ✅ | ✅ |
| D. 编译 / 时序 / 资源 | ✅ | ✅ |
| **E. JTAG 下载** | ❌ | ✅ **本次完成** |
| **F. 上板肉眼画面** | ❌ | ❌ **仍未确认** |

> E 级通过只说明"位流下载成功"，**不证明画面正确**。

---

## 3. 实测日志（原文）

### 3.1 烧录前：JTAG 链扫描（确认板子在线）

```
Scanning JTAG IDCODES at ftdi://0x0403:0x6011:2:c/2
Found 1 devices in chain
JTAG device chain:
	Device 1.  0x10660a79 (Ti60)
```

JTAG ID `0x10660A79` 与 `README.md` 记录一致。

### 3.2 烧录

```
Using board profile: 'Generic Board Profile Using FT4232H' for pinout connection
Connecting to JTAG_TAP: efx_ti
jtag programming started!
JTAG Programming on ftdi://0x0403:0x6011:2:c/2
Programming 'candidate_bitstreams\algo_canny_full_20260926_1954.bit' via JTAG at freq 6.0 MHz
Device ID read from JTAG: 0x10660A79
... finished with JTAG programming
```

退出码 `0`。**JTAG 下载成功。**

---

## 4. 复现命令

```bat
cd /d D:\FPGA_Project\imx219_pyrtl
tools\flash_candidate.bat candidate_bitstreams\algo_canny_full_20260926_1954.bit
```

### 4.1 本机特有的两个环境坑（已封装进脚本）

1. **`EFINITY_HOME` 被设成了用户级环境变量**（值 `C:/Efinity/2026.1`）。
   而 `C:\Efinity\2026.1\pgm\bin\ftdi_pgm.bat` 开头是：

   ```bat
   if not defined EFINITY_HOME ( call "%~dp0\..\..\bin\setup.bat" )
   ```

   于是它**跳过** `setup.bat` → `PYTHONHOME` 没被设置 → 内置 Python 直接崩：

   ```
   Fatal Python error: init_fs_encoding: failed to get the Python codec of the filesystem encoding
   ModuleNotFoundError: No module named 'encodings'
   ```

   解决：**先显式 `call setup.bat`**，再调 `ftdi_pgm.bat`。

2. **`EFINITY_USER_DIR` 需要手动设**。`setup.bat` 只设了 `EFINITY_USER_DIR_INI`
   （指向 `%LOCALAPPDATA%\efinity\user_dir.ini`），而板卡 profile 加载器
   （`efx_pgm/efx_hw_common/boards.py`）直接读 `EFINITY_USER_DIR`，
   否则报 `KeyError: 'EFINITY_USER_DIR'`。
   本机该值 = `C:\Users\HUAWEI\.efinity`（从 `user_dir.ini` 里读到）。

### 4.2 脚本文件必须是纯 ASCII

cmd.exe 按 OEM 代码页（本机 GBK）解析 `.bat`，中文注释会变成
`'xx:' is not recognized as an internal or external command`。
脚本里的注释因此全用英文。

---

## 5. 尚未确认的（等屏幕观察）

以下**全部未确认**，需要有人看着屏幕回答：

| 观察点 | 期望现象 | 结论 |
| --- | --- | --- |
| 是否能出图 | HDMI 有画面 | 待填 |
| 显示模式轮换 | 每 128 帧在 0/1/2/3 之间切换（本版新增的演示行为） | 待填 |
| 分屏口径 | mode 0 时左右两半是**同一完整视野**（左灰度/右边缘） | 待填 |
| Canny 效果 | 人脸/棋盘轮廓应比旧 Sobel 版干净、少杂点 | 待填 |
| 帧间闪烁 | 平坦白墙是否闪白点（已知现象，`temporal_blend` 未 RTL 化） | 待填 |
| 图像最外 7 行/列 | 无边缘（流式架构固有边界，非 bug） | 待填 |

> 如果**完全没有画面**：先用 `known_good/edge_detect_720p_verified.bit`
> 烧一次确认"屏幕 + 线材 + 摄像头"链路本身正常，再回到本版排错。

---

## 6. 回退方法

```bat
:: 回到已肉眼验证过的恢复位流
tools\flash_candidate.bat known_good\edge_detect_720p_verified.bit
```

JTAG 下载是**易失**的：断电后需重新下载；按 CRESET_N 可能重新加载 Flash 里的旧设计。

`known_good/edge_detect_720p_verified.bit` 未被本次操作改动：
SHA-256 仍为 `146D627FF082B7DF383068A9511EAE1B5758CDEABE6B7562E0DA2A6755D0A355`。
